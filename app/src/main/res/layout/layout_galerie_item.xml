<android.support.v7.widget.CardView
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/card_view"
    android:layout_width="@dimen/card_view_width"
    android:layout_height="wrap_content"
    card_view:cardUseCompatPadding="true"
    card_view:cardCornerRadius="@dimen/card_view_corner_radius"
    android:layout_marginBottom="@dimen/card_view_margin_bottom">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/sketch_image"
            android:layout_width="@dimen/card_view_image_size"
            android:layout_height="@dimen/card_view_image_size"
            android:contentDescription="@string/img_content_description"
            android:scaleType="centerCrop" />

        <TextView
            android:id="@+id/sketch_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/card_view_text_size"
            android:textColor="@color/colorAccent"
            android:gravity="center"
            android:layout_below="@+id/sketch_image"
            android:paddingBottom="@dimen/card_view_text_padding_vertical"
            android:paddingTop="@dimen/card_view_text_padding_vertical"
            android:layout_alignParentBottom="true"
            android:background="@color/colorPrimaryDark"/>

    </RelativeLayout>

</android.support.v7.widget.CardView>